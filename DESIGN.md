# 밈능검 — 밈 능력 검정시험(비공식) · DESIGN SSOT

> 이 문서가 제품의 단일 진실원천이다. 코드와 충돌하면 이 문서를 기준으로 코드를 고친다.

## 1. 프레이밍 (game-framing)

| 항목 | 내용 |
|---|---|
| 플레이어 판타지 | "나는 유행을 아는 사람이다" — 밈 감각을 공인(패러디)받는 경험 |
| 장르 | 데일리 퀴즈 아케이드 (지식 × 순발력) |
| 타겟 | 1차: 한국 20~30대 카톡/인스타 사용자 · 2차: "요즘 애들 말 모르겠다"는 30대 후반+ |
| 핵심 감정 | 자랑스러움(고득점) / 웃긴 현타(저득점) — **양쪽 모두 공유 동기가 되도록 설계** |
| 디자인 필러 | ① 3분 완성 ② 오답도 웃기다 ③ 결과는 무조건 공유하고 싶다 |
| 플랫폼 | 모바일 웹 우선 (카톡 인앱 브라우저에서 완벽 동작) |
| 금지 범위 | 로그인, 백엔드, 개인정보 수집, 특정인 비하 문항 |

## 2. 유행 전파 심리 → 설계 장치 매핑 (이 게임의 존재 이유)

| 심리 기제 | 근거 사례 | 밈능검의 장치 |
|---|---|---|
| 정체성 신호 보내기 | MBTI, 성향 테스트 공유 문화 | 급수·칭호·밈연령이 박힌 성적통지표 PNG |
| 공통 화제 (동조) | Wordle "오늘 어려웠다" 대화 | 매일 전원 동일 12문항 (날짜 시드) |
| 무스포일러 자랑 + 호기심 갭 | Wordle 이모지 그리드 | 🟩🟨🟥⬛ 그리드 (정답 노출 없이 난이도만 전달) |
| 자기비하 유머 공유 | "나 늙었나 봐ㅋㅋ" 밈 | 6급 "밈 화석", 재응시 "N수생" 스탬프 |
| 도전장 (경쟁 소환) | 친구 태그 문화 | 공유 문구에 "넌 몇 급?" 도발 라인 자동 삽입 |
| 손실 회피 (습관화) | Wordle/듀오링고 스트릭 | 🔥 연속 응시 스트릭 |
| 세대 놀이 | "요즘 애들/한물간 사람" 티키타카 | 연차별(2024/2025/2026) 정답률 + 밈연령 측정 |
| 저마찰 전파 | 링크 하나로 3분 | 무설치·무로그인·모바일 퍼스트 |

**핵심 창작 결정: 오답 선지가 개그다.** 문제 자체가 스크린샷 소재가 되어 결과 카드 외에 두 번째 공유 표면을 만든다.

## 3. 루프 (game-loop-design)

- **코어 루프(초 단위)**: 문항 제시 → 제한시간 압박(8→6→4.5초) → 4지선다 선택 → 즉각 정오 피드백 + 콤보/점수 → 다음 문항
- **세션 루프(3분)**: 12문항 → 채점 연출(도장 쾅) → 급수·밈연령·칭호 → 공유/저장 → 오답노트(해설 학습)
- **메타 루프(일 단위)**: 자정(KST) 새 회차 → 스트릭 유지 욕구 → 재방문. 재응시는 허용하되 "N수생" 낙인으로 첫 성적의 무게 유지
- **보상 루프**: 점수(즉시) → 급수/칭호(세션) → 스트릭(장기) → 단톡방 리액션(사회적, 게임 외부)

## 4. 시스템 명세 (game-systems-converge / game-build-spec)

### 출제 — 오직 최신 밈만, 날짜에 따라 자동 최신화 (핵심)
- 각 문항에 **유행 시기 `t:"YYYY-MM"`** 태그. 데일리 출제 풀 = **최근 2개 연도(올해·작년) 문항만** (`buildPool()`: `year >= 오늘연도−1`, 24개 미만이면 최근순 24개로 보강해 빈 출제 방지)
- **자동 최신화 원리**: 코드 수정 없이 두 방향으로 저절로 최신화된다
  1) 새 밈을 `t`에 최신 날짜로 추가하면 → 데일리 풀에 즉시 편입, 오래된 밈은 상대적으로 밀려남
  2) 해가 바뀌면 기준 연도(`오늘연도−1`)가 자동으로 올라가 → 2년 지난 밈은 데일리에서 자동 탈락
- 2026-07 기준: 풀=2025·2026 밈 35개, **2024 국민밈 18개는 데일리에서 자동 제외**(무한급수 연습 모드에서도 동일 풀 사용)
- 데일리: `seed = YYYYMMDD(KST)` → 풀에서 시드 셔플 → 12문항 → 난이도(d1~3) 오름차순
- 티어(e)는 레거시 필드(색상용 참고). **최신성 판정은 `t` 날짜 기준**으로 런타임 계산(`freshClass`/`freshLabel`, `POOL_NEWEST_YEAR` 대비). 결과 막대·시험 배지·밈연령 모두 `t`에서 파생
- 콘텐츠 출처: 2026 상반기 트렌드 리서치(오상추 난리자베스, 윤정아 챌린지, 젬민이, 셋로그 등) + 2025 결산 자료 교차검증
- **풀 보충은 사람이 한다**(자동 스크래핑 아님): 밈 뜻·오답 개그·부적절 필터는 편집 판단이 필요. §5 리프레시 규칙 참고
- 선지 순서도 시드 셔플 → **전국 동일 시험지** (스크린샷 대화 호환성)
- 회차 = 2026-07-06(제1회)부터의 경과일 + 1

### 판정
- 점수 = 정답 100 + 남은시간 보너스(최대 50) + 콤보 보너스(10×min(연속-1, 5)) — 이론상 만점 ≈ 2,250
- 급수: 1급 ≥1800 / 2급 ≥1500 / 3급 ≥1200 / 4급 ≥900 / 5급 ≥600 / 6급 <600
- 밈연령 = clamp(42 − 24×acc(최신연도) − 8×acc(다음연도) − 4×acc(그다음), 15, 52) · 연도 버킷은 출제된 문항의 `t`에서 동적 계산 · 전답 오답 시 "측정 불가"
  - **축을 뒤집음**: 최신 연도 밈을 맞힐수록 어리게(=최전선), 오래된 연도만 알수록 나이 들게(=한 박자 늦음). "옛날 밈=나이 많음"이 아니라 "못 따라감=나이 많음".
- 무한급수 모드: 목숨 3개, 제한시간 6초에서 문항당 −0.12초(하한 2.8초), 최고 점수 localStorage

### 공유
- 텍스트: `밈능검 제N회 | X급 칭호 | 밈연령 Y세` + 이모지 그리드 + 도발 문구 + URL (Web Share API → 클립보드 폴백)
- 이미지: Canvas 1080×1350 성적통지표 (관공서 증서 패러디, 붉은 검인, N수생 워터마크) → 파일 공유 → 다운로드 폴백

### 저장 (localStorage, 키 프리픽스 `mng:`)
`daily:{date}` 첫 응시 성적(정식)+attempts+latest / `streak` {n,last} / `best-endless` / `name`

### 테스트베드
`?debug=1` 제한시간 4배 + 정답 점선 표시 · `?exam=YYYYMMDD` 특정 회차 미리보기

## 5. 콘텐츠 가이드라인
- 문항은 **표현(용어) 밈** 위주. 특정인 조롱·비하·저속 표현 금지, 인물 언급은 긍정적 밈만
- 해설(n)은 "뜻 + 시기/맥락 한 줄". 확실하지 않은 기원은 단정하지 않는다
- 오답 선지 3개 중 최소 1개는 순수 개그, 1개는 그럴듯한 함정
- 신규 밈 추가 = 배열에 **`t`를 최신 날짜로 넣어 append만** 하면 끝. 데일리 풀 편입·오래된 밈 자동 탈락은 코드가 알아서 처리(날짜 기반). e 필드는 아무 값이나 무방(색상 참고용, 판정은 t 기준)
- **밈은 빨리 식으므로 분기마다 리프레시** 권장: 갓 뜬 밈을 최신 `t`로 추가, 확실히 죽은 밈은 삭제. 강등/승격은 이제 수동 조정 불필요(날짜가 처리)
- 밈 수명이 짧으니(1~2주) 문항은 "그 주의 신상"보다 **분기~반기 생존한 밈** 위주로 골라 오출제 리스크를 줄인다
- **자동화(구현됨)**: `.github/workflows/daily-meme-refresh.yml`가 매일 00:30 KST에 돌며 Claude가 신상 밈을 조사·작성한다. 무인 배포의 품질 리스크는 **자동 게이트**로 방어: ① 스키마·중복·금지어 검증(scripts/refresh-memes.mjs) ② 통과 문항이 있을 때만 index.html 수정 ③ **배포 전 Playwright E2E**(test/e2e.mjs) 통과 시에만 Netlify 배포, 실패 시 롤백 ④ 새 밈 없으면 no-op(빈 커밋 없음). 사람 검수 대신 "검증+E2E+최신-only 날짜필터"가 안전망. 완전 무인이 부담되면 워크플로의 배포 스텝을 PR 생성으로 바꿔 검수 게이트로 전환 가능.

## 6. 일일 자동 최신화 파이프라인 (구현됨)
- **트리거**: `.github/workflows/daily-meme-refresh.yml` — cron `30 15 * * *`(00:30 KST) + 수동(workflow_dispatch)
- **1) 리서치**: `scripts/refresh-memes.mjs`가 Claude(`claude-sonnet-5` 기본, `vars.REFRESH_MODEL`로 변경) + `web_search` 서버툴로 최근 1~2개월 신상 한국 밈 조사. 기존 문항 목록을 제외 리스트로 전달(중복 방지)
- **2) 작성**: 구조화 출력(`output_config.format` json_schema)으로 DESIGN 규칙(정답=c[0], 4지선다, 개그 오답, 해설, `t`=YYYY-MM) 준수 문항 생성(최대 `MAX_NEW`=2)
- **3) 검증 게이트**: 4지선다·중복(문제/정답)·`t` 최신성·난이도·금지어(BLOCKLIST) 체크. 통과분만 `t`에 최신 날짜로 index.html에 삽입. 0개면 파일 무변경
- **4) 배포 게이트**: index.html이 바뀌면 Chromium 설치→로컬 서버→`test/e2e.mjs`(플레이·채점·성적표·오답노트·콘솔에러0·최신-only) 실행. **통과해야만** `netlify deploy --prod`. 실패 시 `git checkout`으로 롤백
- **5) 커밋**: 배포 후 index.html 변경을 봇 계정으로 커밋·푸시(감사 로그)
- **필요 시크릿**(레포 Settings→Secrets and variables→Actions): `ANTHROPIC_API_KEY`, `NETLIFY_AUTH_TOKEN`, `NETLIFY_SITE_ID`(=`0ec9847a-6b75-451e-8691-eb62d6cf5270`). 미설정 시 워크플로는 안전하게 no-op
- **비용**: 소넷5 + 웹검색 몇 건 + 소량 토큰 = 일 수 센트 수준. 밈이 매일 새로 뜨지 않으므로 대부분의 날은 no-op(변경·배포 없음)

## 7. 수익화 (MONETIZE.md 참조)
결과 화면 광고 슬롯(`CONFIG.ADS`) 기본 비활성 → AdFit/AdSense 승인 후 활성화. Steam은 steam/STEAM.md.

## 8. KPI (검증 가설)
- D1 공유율(공유 버튼 클릭/응시 완료) ≥ 15%면 바이럴 루프 유효
- 재방문(스트릭 2일+) ≥ 25%면 데일리 포맷 유효
- 위 미달 시: 공유 문구 A/B, 문항 난이도 조정(1급이 너무 흔하면 변별력 상실)
